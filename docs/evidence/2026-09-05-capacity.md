# Local ingress capacity baseline — 2026-09-05

Status: **measured pass for the local baseline**, not a full-platform production capacity claim.

## Reproduction and boundary

macOS ARM64; Colima `kind-lab`, 4 vCPU/8 GiB; Kind Kubernetes 1.36.1 with one control-plane/two workers, Calico and ingress-nginx. Local Helm values: two API replicas, one worker, one PostgreSQL; HPA disabled. Requests/limits per API: 50m/64Mi → 500m/128Mi; worker: 100m/64Mi → 750m/128Mi; PostgreSQL: 100m/256Mi → 1 CPU/512Mi. Pool maximum five connections per app process; work units 25,000 per job.

Image: `taskflow:local-20260905175151`, local image index `sha256:3cec2a1ec45a72ceab5384c999bf0d72d4afb3de8dc8933054a220549ecd29ed`. The application source matches initial repository commit `e00fa001d6b0d323a2df90514321aa822d549b1d`; this pre-commit local build does not embed that SHA in `/version`. It is not the later GHCR artifact. The full observability stack and OTLP export were **not** active during this baseline.

Command, from the repository root:

```bash
k6 run --quiet --no-usage-report --env BASE_URL=http://taskflow.localhost:8080 --env PROFILE=acceptance --summary-export .cache/reports/k6-acceptance.json tests/load/taskflow.js
```

Traffic went through localhost ingress, not a Service port-forward. K6 exited 0. First database submission: `2026-09-05T18:02:56.963255Z`; last: `18:12:56.952048Z`; last completion: `18:12:56.954419Z`. The final database query found only `succeeded = 30001`, with zero queued/running/failed jobs.

| Measurement | Observed |
| --- | --- |
| Requests / valid 202 acceptance | 30,001 / 100% |
| Measured request rate | 50.0045/s |
| HTTP failure rate | 0% |
| Generator dropped iterations | 0 |
| Submission average / median | 1.422 / 1.385 ms |
| Submission p90 / p95 / maximum | 1.638 / 1.758 / 13.463 ms |
| End-to-end job p95 / maximum, computed from DB timestamps | 243.865 / 934.143 ms |
| Final terminal job count | 30,001 succeeded |

During-run spot samples, **not peaks**: APIs about 9–10m CPU and 7Mi each; worker about 91m/7Mi; PostgreSQL about 37m/42Mi. VM memory sample: 2,014 MiB used and 5,907 MiB available. Queue spot samples stayed around 8–11 waiting jobs; these samples do not prove the exact peak backlog. The final completion followed the last submission by approximately 2.4ms; no backlog remained when checked.

All configured thresholds passed (≥99.5% acceptance, <0.5% HTTP failure, p95 <300ms and zero drops). API submission and job completion latency are deliberately reported separately. p99, continuous CPU throttling, pool-acquisition pressure and a saturation knee were not collected; this is one acceptance run, not a tuning comparison. Repeat with the full telemetry stack, immutable GHCR digest and additional ramp/spike/soak profiles before extending the claim.
